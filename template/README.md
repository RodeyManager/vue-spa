```javascript
  // install gupack global
  yarn global install gupack
  // npm
  // npm install gupack -g

  // init install npm packages
  yarn install

  // development
  yarn start

  // stage
  yarn run stg

  // production
  yarn build
```
